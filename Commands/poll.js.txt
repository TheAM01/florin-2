import {SlashCommandBuilder} from "discord.js";
import promises from "node:timers/promises";
const wait = promises.setTimeout

export default {

    data: new SlashCommandBuilder()
        .setName('poll')
        .setDescription('Carries out a poll')
        .addStringOption(o => 
            o.setName("Poll question")
            .setDescription("Ask your poll or question")
            .setRequired(true)
            )
        .addStringOption(o => 
            o.setName("Option 1")
            .setDescription("First option/answer for your poll")
            .setRequired(true)
            )
        .addStringOption(o => 
            o.setName("Option 2")
            .setDescription("2nd option/answer for your poll")
            .setRequired(true)
            ),

    async execute(interaction, client) {
        
    },

}